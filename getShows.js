/**
 * This should not need to be run.
 * It just grabs a list of supported shows from showRSS
 */

var shows = require('./shows.js'),
	fs = require('fs'),
	path = require('path');

shows.getShows(function(err, shows){
	if 
});
